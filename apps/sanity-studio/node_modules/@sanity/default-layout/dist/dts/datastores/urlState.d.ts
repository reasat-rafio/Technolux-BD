import { Observable } from 'rxjs';
interface SnapshotStateEvent {
    type: 'snapshot';
    intent: {
        name: string;
        params: {
            [key: string]: string;
        };
    };
    isNotFound: boolean;
    state: Record<string, unknown>;
}
interface ErrorStateEvent {
    type: 'error';
    error: Error;
}
declare type StateEvent = SnapshotStateEvent | ErrorStateEvent;
export declare function navigate(newUrl: string, options: {
    replace: boolean;
}): void;
export declare const state: Observable<StateEvent>;
export declare function isSnapshotEvent(event: StateEvent): event is SnapshotStateEvent;
export {};
//# sourceMappingURL=urlState.d.ts.map